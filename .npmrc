# NOTE: Do not disable optional dependencies. Some packages (e.g. `lightningcss`)
# ship platform-specific native binaries via optional deps and Next/Tailwind builds
# can fail if they are omitted.

# Hoist to avoid nested node_modules
hoist=true

